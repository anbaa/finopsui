# ============================================================
# Environment variables for the new deployment
# Copy this to .env.local and fill in values as steps complete
# ============================================================

# API Gateway HTTP API (filled after Step 4)
NEXT_PUBLIC_BFF_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com

# API Gateway WebSocket API (filled after Step 5)
NEXT_PUBLIC_WS_URL=wss://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/prod

# Cognito — already known (Step 1)
NEXT_PUBLIC_COGNITO_USER_POOL_ID=ap-southeast-2_TING9KcBS
NEXT_PUBLIC_COGNITO_CLIENT_ID=4u526tl9qftouvrt1fgvmblojc
NEXT_PUBLIC_COGNITO_REGION=ap-southeast-2

# Legacy (same as BFF URL — can be same value)
NEXT_PUBLIC_API_URL=https://XXXXXXXXXX.execute-api.ap-southeast-2.amazonaws.com/prod
